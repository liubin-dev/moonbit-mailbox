import {
  "localreview/mailbox" @lib,
}

supported_targets = "js"

pkgtype(kind: "executable")

options(
  link: {
    "js": {
      "exports": [ "run", "maildir_name", "maildir_flags" ],
      "format": "esm",
    },
  },
)
