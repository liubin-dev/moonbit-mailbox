# 复核说明 · 2026-09-22

当前建议：**条件复审**。

## 针对三类反馈的处理

1. **仓库不能克隆**：本项目地址为 https://github.com/liubin-dev/moonbit-mailbox。2026-09-22 匿名新克隆成功；默认分支 `main`，核验公开提交 `78598c07d673889d04daea172876f2df2b1f0e34`。本轮源码修订仅在本地，尚未推送；此记录不证明当时报名表中的地址正确，也不证明新修订已上线。
2. **生态已有实现**：mailkit_12314、MoonMIME 等已经提供 MIME 解析；本项目保留这部分为辅助功能，不以首个 MIME 库申报。重点是 mbox/Maildir 归档存取和与 Python mailbox 的磁盘交换流程。 不再用同名搜索未命中概括整个技术类别为空白。
3. **选题简单、过窄或用途不足**：需要 mbox/Maildir 原始邮件归档交换时评估，MIME 解析只是辅助；新 CLI 让库可以完成实际文件任务。 与 MIME 库有重叠且事务/断电边界有限；本轮合成邮件不是真实客户归档。

## 本地实际变化

- 将入口改成当前版本、模块名、完整自身 URL、具体输入/操作/输出；旧记录单独存档。
- 增加可运行本地任务及来源性质标注，不把离线样例冒充第三方服务器、客户或生产验证。
- 新增 import/list/flags/export CLI 以 SHA256 展示原始字节保持；本轮验证重复键、输出不覆盖和非 UTF-8 字节。先前 Python mailbox 双向磁盘交换报告独立保留。

## 仍不能声称已解决

无 mbox 磁盘事务/锁；Maildir 没有目录 fsync 或断电持久性保证，其他客户端未必遵守本适配器锁。附件名称是显示文本，不能直接当输出路径。

本次修订不能保证初审通过。对于已驳回的选题，只修复链接或材料不等于解除价值异议；没有具体需求支撑时，应暂缓复申或评估换题。项目作者、参赛人及账号归属仍由团队按真实关系填写，本文不代填身份或生成使用方证明。
